import 'package:flutter/material.dart';

class Doctors_Screen extends StatelessWidget {
  const Doctors_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is the Doctors screen.'));
  }
}