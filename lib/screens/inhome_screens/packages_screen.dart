import 'package:flutter/material.dart';

class Packages_Screen extends StatelessWidget {
  const Packages_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is the Packages screen.'));
  }
}